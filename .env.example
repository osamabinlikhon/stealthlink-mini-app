# StealthLink Mini App Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Telegram Bot Token from @BotFather
# Get your bot token by messaging @BotFather and creating a new bot
BOT_TOKEN=your_telegram_bot_token_here

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================

# HTTP Server Port (default: 3000)
PORT=3000

# WebSocket Server Port (default: 3001)
WS_PORT=3001

# Environment (development/production)
NODE_ENV=development

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Server domain for CORS (auto-detected if not set)
# SERVER_DOMAIN=your-domain.com

# Maximum room participants (default: 2)
MAX_ROOM_PARTICIPANTS=2

# Message timeout in milliseconds (default: 15000 = 15 seconds)
MESSAGE_TIMEOUT_MS=15000

# Room cleanup timeout in milliseconds (default: 3600000 = 1 hour)
ROOM_CLEANUP_TIMEOUT_MS=3600000

# Rate limiting - max messages per minute per user (default: 30)
MESSAGE_RATE_LIMIT=30

# Maximum message length in characters (default: 1000)
MAX_MESSAGE_LENGTH=1000

# WebSocket ping interval in milliseconds (default: 30000 = 30 seconds)
WS_PING_INTERVAL=30000

# Enable debug logging (true/false)
DEBUG_MODE=false

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# CORS allowed origins (comma-separated)
# Leave empty to allow all origins (not recommended for production)
CORS_ORIGINS=

# WebSocket connection timeout in milliseconds (default: 5000)
WS_CONNECTION_TIMEOUT=5000

# Session timeout in milliseconds (default: 300000 = 5 minutes)
SESSION_TIMEOUT_MS=300000

# ============================================================================
# DEPLOYMENT SPECIFIC
# ============================================================================

# SSL/TLS Configuration (for production)
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/key.pem

# Reverse proxy headers (if behind nginx/load balancer)
# TRUST_PROXY=true

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================================================
# TELEGRAM MINI APP SPECIFIC
# ============================================================================

# Mini App URL (used for bot web_app configuration)
# This should be the HTTPS URL where your Mini App is hosted
MINI_APP_URL=https://your-domain.com

# Telegram Web App theme colors (hex values without #)
# Leave empty to use default Telegram colors
# THEME_PRIMARY_COLOR=10B981
# THEME_BACKGROUND_COLOR=000000
# THEME_ACCENT_COLOR=10B981

# ============================================================================
# DATABASE (Future Enhancement)
# ============================================================================

# For future database integration (currently using in-memory storage)
# DATABASE_URL=postgresql://user:password@localhost:5432/stealth_chat
# REDIS_URL=redis://localhost:6379

# ============================================================================
# MONITORING & ANALYTICS (Future Enhancement)
# ============================================================================

# Analytics tracking ID
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Error tracking service (Sentry, etc.)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Health check endpoint authentication
# HEALTH_CHECK_TOKEN=your_health_check_token

# ============================================================================
# BACKUP & MAINTENANCE
# ============================================================================

# Auto-backup configuration (if implemented)
# BACKUP_ENABLED=false
# BACKUP_INTERVAL_HOURS=24
# BACKUP_RETENTION_DAYS=7

# ============================================================================
# NOTES
# ============================================================================

# 1. NEVER commit the .env file with real values to version control
# 2. Use strong, unique values for BOT_TOKEN
# 3. In production, set NODE_ENV=production and enable SSL
# 4. Consider using a process manager like PM2 for production deployment
# 5. Monitor your bot usage through @BotFather analytics
# 6. Regularly rotate your BOT_TOKEN and other sensitive values

# ============================================================================
# EXAMPLE CONFIGURATION
# ============================================================================

# Development Example:
# BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
# PORT=3000
# WS_PORT=3001
# NODE_ENV=development
# DEBUG_MODE=true
# MINI_APP_URL=http://localhost:3000

# Production Example:
# BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
# PORT=3000
# WS_PORT=3001
# NODE_ENV=production
# MINI_APP_URL=https://your-secure-domain.com
# CORS_ORIGINS=https://web.telegram.org,https://your-domain.com
# SSL_CERT_PATH=/etc/ssl/certs/your-cert.pem
# SSL_KEY_PATH=/etc/ssl/private/your-key.pem